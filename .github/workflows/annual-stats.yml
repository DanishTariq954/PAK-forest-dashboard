name: Annual Hansen Stats Refresh

on:
  schedule:
    - cron: "0 6 1 2 *"
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  annual-stats:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - run: pip install earthengine-api

      - name: Run annual stats update
        env:
          EE_SERVICE_ACCOUNT_KEY: ${{ secrets.EE_SERVICE_ACCOUNT_KEY }}
          PYTHONUNBUFFERED: "1"
        run: python -u scripts/update_annual_stats.py

      - name: Commit updated annual data
        run: |
          git config user.name "forest-watch-bot"
          git config user.email "actions@github.com"
          git add docs/data/annual_stats.json
          git diff --quiet --cached || git commit -m "Annual stats refresh: $(date -u +'%Y-%m-%d')"
          git push
